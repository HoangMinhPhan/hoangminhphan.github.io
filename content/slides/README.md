Create a folder inside the folder `slides`. Each created folder is a separate slide.

A slide is written in Markdown format.

To attach the created slide to a blog, add a key-value pair "slides: folder_of_a_separate_slide" in the frontmatter
